[SCRIPT]

# 🧹 Bersihkan semua cache Laravel
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

# ⚙️ Optimasi ulang setelah pembersihan
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 🔄 Refresh autoload dan optimize composer
composer dump-autoload -o

# 🧪 Opsional: Cek environment & versi Laravel
php artisan env
php artisan --version

[PENJELASAN]

# 🧹 Bersihkan semua cache Laravel
php artisan cache:clear         # Menghapus cache aplikasi
php artisan config:clear        # Menghapus cache konfigurasi
php artisan route:clear         # Menghapus cache rute
php artisan view:clear          # Menghapus cache tampilan Blade

# ⚙️ Optimasi ulang setelah pembersihan
php artisan config:cache        # Membuat ulang cache konfigurasi
php artisan route:cache         # Membuat ulang cache rute
php artisan view:cache          # Membuat cache tampilan Blade (opsional)

# 🔄 Refresh autoload dan optimize composer
composer dump-autoload -o       # Optimasi autoload Composer

# 🧪 Opsional: Cek environment & versi Laravel
php artisan env                 # Menampilkan environment aktif
php artisan --version           # Menampilkan versi Laravel

==========================================================================